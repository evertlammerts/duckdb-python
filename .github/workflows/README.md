Tooling:
- Astral UV: to create virtual environments, install dependency groups from diverse indexes, run tools, build the
  sdist, etc.
- CIbuildwheel: to build wheels for different archs and python versions on different platforms.

